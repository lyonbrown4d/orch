version: "3"

vars:
  GORELEASER_VER: v2.12.2

tasks:
  tidy:
    desc: "Go: tidy modules"
    cmds:
      - go mod tidy

  test:
    desc: "Go: run tests"
    cmds:
      - go test ./...

  lint:
    desc: "Go: run golangci-lint"
    cmds:
      - golangci-lint run ./... --allow-serial-runners

  fmt:
    desc: "Go: format (gofmt)"
    cmds:
      - gofmt -w .

  run-cli:
    desc: "Run orch CLI (Go)"
    cmds:
      - go run ./cmd/orch-cli {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  run-server:
    desc: "Run orch server (Go)"
    cmds:
      - go run ./cmd/orch-server {{.SERVER_ARGS}}
    vars:
      SERVER_ARGS: '{{default "" .SERVER_ARGS}}'

  run-lsp:
    desc: "Run orch language server (stdio LSP)"
    cmds:
      - go run ./cmd/orch-lsp {{.LSP_ARGS}}
    vars:
      LSP_ARGS: '{{default "" .LSP_ARGS}}'

  run-vpn:
    desc: "Run orch-vpn workstation daemon (dev)"
    cmds:
      - go run ./cmd/orch-vpn {{.VPN_ARGS}}
    vars:
      VPN_ARGS: '{{default "" .VPN_ARGS}}'

  smoke:local-docker:
    desc: "Run a local single-node Docker deploy smoke test"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-smoke.ps1 {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:local-docker-dns:
    desc: "Run a local Docker workload DNS smoke test"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-dns-smoke.ps1 {{.CLI_ARGS}}

  smoke:local-docker-worker-dispatch:
    desc: "Run a local two-server Docker worker dispatch smoke test"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-worker-dispatch-smoke.ps1 {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:local-podman:
    desc: "Run a local single-node Podman deploy smoke test"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-smoke.ps1 -ContainerRuntime podman -Manifest examples/local-podman-smoke.yaml {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:local-podman-dns:
    desc: "Run a local Podman workload DNS smoke test"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-dns-smoke.ps1 -ContainerRuntime podman -Manifest examples/local-podman-dns-smoke.yaml {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:local-podman-worker-dispatch:
    desc: "Run a local two-server Podman worker dispatch smoke test"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-worker-dispatch-smoke.ps1 -ContainerRuntime podman -Manifest examples/local-podman-worker-dispatch.yaml {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:local-container-runtimes:
    desc: "Run available local runtime smoke checks (Docker and Podman)"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/run-container-runtime-smokes.ps1 {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:local-raft-forwarding:
    desc: "Run a local three-node Raft forwarding smoke test"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-raft-forwarding-smoke.ps1 {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'
  smoke:docker-raft-ingress:
    desc: "Run a Docker 3-node Raft cluster smoke with whoami behind ingress"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-ingress-smoke.ps1 {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'


  smoke:docker-raft-placement:
    desc: "Run a Docker 3-node Raft placement deploy/ingress smoke with a shared Docker daemon"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario placement {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:docker-raft-placement-dind:
    desc: "Run the placement Raft migrate/rebalance/failover smoke with one Docker-in-Docker daemon per node"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario placement -RuntimeIsolation dind {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:docker-raft-rollout:
    desc: "Run a Docker 3-node Raft rollout auto-rollback smoke with whoami"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario rollout {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'
  smoke:docker-raft-nextcloud:
    desc: "Run a Docker 3-node Raft cluster smoke with Nextcloud, Postgres, and Redis"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario nextcloud {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:docker-raft-seaweed:
    desc: "Run a Docker 3-node Raft cluster smoke with SeaweedFS master, volume, and filer"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario seaweed {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'
  smoke:docker-raft-stack-suite:
    desc: "Run stacked Docker Raft stack app suite: placement and rollout"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario placement {{.CLI_ARGS}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario rollout {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:docker-raft-stack-suite-full:
    desc: "Run full Docker Raft stack app suite: placement, rollout, nextcloud, and seaweed"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario placement {{.CLI_ARGS}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario rollout {{.CLI_ARGS}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario nextcloud {{.CLI_ARGS}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario seaweed {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:docker-raft-stack-suite-dind:
    desc: "Run Docker Raft dind stack suite: placement movement and rollout"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario placement -RuntimeIsolation dind {{.CLI_ARGS}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario rollout -RuntimeIsolation dind {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:docker-raft-stack-suite-full-dind:
    desc: "Run Docker Raft full dind stack suite: placement, rollout, nextcloud, and seaweed"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario placement -RuntimeIsolation dind {{.CLI_ARGS}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario rollout -RuntimeIsolation dind {{.CLI_ARGS}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario nextcloud -RuntimeIsolation dind {{.CLI_ARGS}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario seaweed -RuntimeIsolation dind {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:full-chain:
    desc: "Run complete local docker deploy -> worker dispatch -> raft forwarding -> shared stack suite"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-smoke.ps1
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-worker-dispatch-smoke.ps1
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-raft-forwarding-smoke.ps1
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario placement -TimeoutSeconds {{.STACK_TIMEOUT}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario rollout -TimeoutSeconds {{.STACK_TIMEOUT}}
    vars:
      STACK_TIMEOUT: '{{default "300" .STACK_TIMEOUT}}'

  smoke:full-chain-full:
    desc: "Run complete local docker deploy -> worker dispatch -> raft forwarding -> full shared stack suite"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-smoke.ps1
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-worker-dispatch-smoke.ps1
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-raft-forwarding-smoke.ps1
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario placement -TimeoutSeconds {{.STACK_TIMEOUT}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario rollout -TimeoutSeconds {{.STACK_TIMEOUT}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario nextcloud -TimeoutSeconds {{.STACK_TIMEOUT}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario seaweed -TimeoutSeconds {{.STACK_TIMEOUT}}
    vars:
      STACK_TIMEOUT: '{{default "300" .STACK_TIMEOUT}}'

  smoke:full-chain-full-dind:
    desc: "Run complete local docker deploy -> worker dispatch -> raft forwarding -> full DinD stack suite"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-smoke.ps1
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-docker-worker-dispatch-smoke.ps1
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/local-raft-forwarding-smoke.ps1
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario placement -RuntimeIsolation dind -TimeoutSeconds {{.STACK_TIMEOUT}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario rollout -RuntimeIsolation dind -TimeoutSeconds {{.STACK_TIMEOUT}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario nextcloud -RuntimeIsolation dind -TimeoutSeconds {{.STACK_TIMEOUT}}
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/docker-raft-app-smoke.ps1 -Scenario seaweed -RuntimeIsolation dind -TimeoutSeconds {{.STACK_TIMEOUT}}
    vars:
      STACK_TIMEOUT: '{{default "300" .STACK_TIMEOUT}}'

  smoke:vagrant:
    desc: "Run a 3-node Vagrant smoke test (deploy -> status -> delete)"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/vagrant/smoke.ps1 {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  smoke:systemd-docker:
    desc: "Run package + systemd install smoke inside a privileged Docker container"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/systemd-docker-smoke.ps1 {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  vagrant:up:
    desc: "Start Vagrant Orch cluster"
    cmds:
      - vagrant up

  vagrant:halt:
    desc: "Halt Vagrant Orch cluster"
    cmds:
      - vagrant halt

  vagrant:destroy:
    desc: "Destroy Vagrant Orch cluster"
    cmds:
      - vagrant destroy -f

  bench:resources:
    desc: "Measure orch-server resource usage for idle, cluster, and scheduling scenarios"
    cmds:
      - pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/bench-orch-resources.ps1 {{.CLI_ARGS}}
    vars:
      CLI_ARGS: '{{default "" .CLI_ARGS}}'

  dsl-parse:
    desc: "Parse deploy YAML into canonical model"
    requires:
      vars: [FILE]
    cmds:
      - go run ./cmd/orch-cli parse --file {{.FILE}} {{if .JSON}}--json{{end}}

  dsl-validate:
    desc: "Validate deploy YAML (exit non-zero on error)"
    requires:
      vars: [FILE]
    cmds:
      - go run ./cmd/orch-cli validate --file {{.FILE}}

  goreleaser-check:
    desc: "Validate .goreleaser.yaml"
    cmds:
      - go run github.com/goreleaser/goreleaser/v2@{{.GORELEASER_VER}} check

  release-snapshot:
    desc: "Archives + deb/rpm/apk snapshot under dist/ (no GitHub publish)"
    cmds:
      - go run github.com/goreleaser/goreleaser/v2@{{.GORELEASER_VER}} release --snapshot --clean

  release-gate:static:
    desc: "Run beta release gate checks that do not require Docker"
    cmds:
      - go mod tidy
      - task lint
      - go test ./...
      - task goreleaser-check
      - task release-snapshot
      - task smoke:local-raft-forwarding


  release-gate:e2e:
    desc: "Run extended full chain e2e smoke suite"
    cmds:
      - task smoke:full-chain-full

  release-gate:
    desc: "Run full beta release gate checks, including local container-runtime smoke tests"
    cmds:
      - task release-gate:static
      - task smoke:local-container-runtimes
