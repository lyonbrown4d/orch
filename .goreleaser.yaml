# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# https://goreleaser.com — check: goreleaser check
# Snapshot (no Git tag): goreleaser release --snapshot --clean
#
# Alternatives (if you prefer not to use GoReleaser):
# - nfpm alone: YAML-only deb/rpm/apk from built binaries — https://nfpm.goreleaser.com
# - fpm: Ruby gem wrapping many package formats (deb/rpm/solaris/etc.)
# - distro packaging: rpmbuild / debhelper on native Linux builders or OBS
version: 2

project_name: orch

before:
  hooks:
    - go mod tidy

builds:
  - id: orch-cli
    main: ./cmd/orch-cli
    binary: orch
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s
      - -w
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.version={{ .Version }}
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.commit={{ .Commit }}
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.date={{ .Date }}

  - id: orch-server
    main: ./cmd/orch-server
    binary: orch-server
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s
      - -w
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.version={{ .Version }}
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.commit={{ .Commit }}
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.date={{ .Date }}

  - id: orch-lsp
    main: ./cmd/orch-lsp
    binary: orch-lsp
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s
      - -w
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.version={{ .Version }}
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.commit={{ .Commit }}
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.date={{ .Date }}

  - id: orch-vpn
    main: ./cmd/orch-vpn
    binary: orch-vpn
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s
      - -w
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.version={{ .Version }}
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.commit={{ .Commit }}
      - -X github.com/lyonbrown4d/orch/internal/buildmeta.date={{ .Date }}

archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_checksums.txt"

release:
  prerelease: auto

snapshot:
  version_template: "{{ incpatch .Version }}-SNAPSHOT-{{.ShortCommit}}"

nfpms:
  - package_name: orch
    file_name_template: "{{ .ConventionalFileName }}"
    vendor: orch
    homepage: https://github.com/lyonbrown4d/orch
    maintainer: orch maintainers <orch@users.noreply.github.com>
    description: orch control plane server and CLI (stateful workload runtime layer).
    license: MIT
    section: utils
    formats:
      - deb
      - rpm
      - apk
    bindir: /usr/bin
    contents:
      - src: packaging/linux/orch-server.service
        dst: /usr/lib/systemd/system/orch-server.service
        file_info:
          mode: 0644
          owner: root
          group: root
      - type: dir
        dst: /etc/orch
        file_info:
          mode: 0755
          owner: root
          group: root
      - src: packaging/linux/orch.env
        dst: /etc/orch/env
        type: config|noreplace
        file_info:
          mode: 0644
          owner: root
          group: root
      - type: dir
        dst: /var/lib/orch
        file_info:
          mode: 0755
          owner: root
          group: root
    scripts:
      postinstall: packaging/linux/postinstall.sh
      preremove: packaging/linux/preremove.sh
      postremove: packaging/linux/postremove.sh
